version: "3.7"

services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    user: root
    volumes:
      - C:\containers\seq:/data
    ports: 
      - "5341:80"
    environment:
      ACCEPT_EULA: "Y"
    networks:
      - dev-network

networks:
  dev-network:
    driver: bridge